{
  "name": "chatsino-server",
  "version": "0.0.1",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "esbuild ./src --bundle --outfile=build/chatsino.js",
    "start": "yarn build && node build/chatsino"
  },
  "dependencies": {
    "esbuild": "^0.15.12",
    "ws": "^8.10.0"
  },
  "optionalDependencies": {
    "bufferutil": "^4.0.7",
    "utf-8-validate": "^5.0.10"
  }
}
